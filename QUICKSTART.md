# ROS2WebLink 快速使用指南 🚀

## ✅ 自动启动已配置！

你的 ROS2WebLink 服务已经配置为**开机自动启动**，重启后无需手动操作即可使用。

---

## 🎮 快速管理命令

### 方式 1: 使用管理脚本（推荐）⭐

```bash
cd /home/guest/ROS2WebLink

# 健康检查（最常用）
./manage_service.sh health

# 查看服务状态
./manage_service.sh status

# 重启服务
./manage_service.sh restart

# 查看实时日志
./manage_service.sh logs

# 查看最近日志
./manage_service.sh logs-last
```

### 方式 2: 使用 systemctl

```bash
# 查看服务状态
systemctl status ros2weblink

# 重启服务（需要 sudo）
sudo systemctl restart ros2weblink

# 查看日志（需要 sudo）
sudo journalctl -u ros2weblink -f
```

---

## 🌐 访问地址

| 服务 | 地址 |
|------|------|
| **Web UI** | http://192.168.100.88:8800 |
| **API 文档** | http://192.168.100.88:8800/docs |
| **健康检查** | http://192.168.100.88:8800/healthz |

---

## 🔧 服务配置

- ✅ **开机自启**: 已启用
- ✅ **自动重启**: 崩溃后 10 秒自动重启
- ✅ **ROS2 环境**: 自动加载 Humble + tron_ros2
- ✅ **tron_commander**: 使用 PID 版本（更精准的停车控制）
- 🚀 **启动延迟**: 10 秒（确保网络和依赖就绪）

---

## 📋 常见场景

### 1️⃣ 重启机器后检查服务
```bash
# 等待开机完成后（约 1-2 分钟）
cd /home/guest/ROS2WebLink && ./manage_service.sh health
```

### 2️⃣ 修改代码后重启服务
```bash
# 停止服务
sudo systemctl stop ros2weblink

# 修改代码...

# 重启服务
sudo systemctl start ros2weblink

# 检查状态
cd /home/guest/ROS2WebLink && ./manage_service.sh health
```

### 3️⃣ 查看为什么服务没启动
```bash
# 查看服务状态
sudo systemctl status ros2weblink

# 查看最近的错误日志
sudo journalctl -u ros2weblink -n 50
```

### 4️⃣ 端口被占用
```bash
# 查看谁占用了 8800 端口
sudo lsof -i:8800

# 杀掉占用进程
sudo kill -9 $(sudo lsof -ti:8800)

# 重启服务
sudo systemctl restart ros2weblink
```

---

## 📚 完整文档

- **详细管理指南**: `AUTOSTART_GUIDE.md`
- **安装指南**: `SETUP_GUIDE.md`
- **管理脚本**: `manage_service.sh`

---

## ⚡ 一行命令健康检查

```bash
cd /home/guest/ROS2WebLink && ./manage_service.sh health
```

预期输出：
```
🏥 健康检查...

✓ 服务状态: 运行中
✓ 端口 8800: 正在监听
  HTTP 健康检查: ✓ OK

✓ 所有检查通过！

访问地址:
  - Web UI:      http://192.168.100.88:8800
  - API 文档:    http://192.168.100.88:8800/docs
  - 健康检查:    http://192.168.100.88:8800/healthz
```

---

## 🎉 总结

1. **开机即用**: 重启后自动启动，无需手动操作
2. **稳定可靠**: 自动重启机制，崩溃后自动恢复
3. **方便管理**: 使用 `manage_service.sh` 脚本快速管理
4. **完整日志**: systemd 日志系统，方便排查问题

**现在你可以放心重启机器，ROS2WebLink 会自动启动！** 🚀
